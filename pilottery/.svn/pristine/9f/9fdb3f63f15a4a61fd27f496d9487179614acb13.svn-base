#ifndef _RNG_CRC16_H_
#define _RNG_CRC16_H_

void crc_calculate(unsigned short* crc, unsigned char* buffer, int length);

// return the calculated crc value of the message buffer 'buf'
// 'len' should not include the last two bytes for crc
unsigned short calc_crc(void *buf, int len);

#endif // _RNG_CRC16_H_