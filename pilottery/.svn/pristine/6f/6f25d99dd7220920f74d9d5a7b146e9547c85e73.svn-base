
FILE(GLOB src_files "*.cpp")

ADD_LIBRARY(gl_KOC7LX SHARED ${src_files} )
TARGET_LINK_LIBRARIES(gl_KOC7LX dl gl frmwk)

#VERSION standfor so version; SOVERSION standfor API version
SET_TARGET_PROPERTIES(gl_KOC7LX PROPERTIES VERSION 1.0 SOVERSION 0)

INSTALL(TARGETS gl_KOC7LX
        RUNTIME DESTINATION task
        LIBRARY DESTINATION libs
        ARCHIVE DESTINATION libs
)
