使用说明：

httpterm.py 是一个采用http传输Json消息的模拟接入商的售票程序

在httpterm.py的文件内，通过修改一下三个字段，设置host的ncpc_http_task的地址和端口，使用的线程数，及采用的http请求方式
url = "192.168.26.160:20080"
thread_num = 1
method = "POST" #GET

程序运行：
python httpterm.py 设置为接入商类型的投注站编号 投注字符串文件名称 

例如：
python httpterm.py 100000121 bet_tty.txt

